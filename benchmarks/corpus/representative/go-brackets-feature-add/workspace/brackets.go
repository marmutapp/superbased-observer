package brackets

// IsBalanced reports whether every bracket in s is properly matched and
// nested. It must support the three bracket pairs (), [], and {}.
//
// FEATURE TO ADD: this is a stub that always returns false, so the
// provided table test fails. Implement the real check (a stack scan is
// the natural approach) in THIS file, without modifying brackets_test.go
// and without adding new files. Keep the exported signature
// `func IsBalanced(s string) bool`.
func IsBalanced(s string) bool {
	return false
}
